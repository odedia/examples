const gaugeGraph = {
  metricName: 'custom.QRcode.instance.activeTime',
  metricTitle: 'Active time',
};

const lineChart = {
  metricName: 'custom.QRcode.instance.ips',
  metricTitle: 'Impressions per second (IPS)',
};

const averageNum = {
  metricName: 'custom.QRcode.instance.sps',
  metricTitle: 'Scans per second (SPS)',
};

export { gaugeGraph, lineChart, averageNum };
