const gaugeGraph = {
  metricName: 'custom.ETL.instance.uptime',
  metricTitle: 'Up time',
};

const lineChart = {
  metricName: 'custom.ETL.instance.rps',
  metricTitle: 'Records per second (RPS)',
};

const averageNum = {
  metricName: 'custom.ETL.instance.running_ratio',
  metricTitle: 'Running Ratio',
};

export { gaugeGraph, lineChart, averageNum };
